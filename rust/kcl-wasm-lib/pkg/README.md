# @taucad/kcl-wasm-lib (`pkg/`)

Tau maintains this directory’s `package.json` (exports + npm name) and copies `bindings/` from `rust/kcl-lib` after `wasm-pack`. The WASM/JS artifacts are produced by `npm run build:wasm` in the `taucad/modeling-app` fork.
